<h1>Tous nos restaurants</h1>

<%- @restaurants.each do |restaurant| -%>
  <section>
    <h2><%= restaurant.name %></h2>
    <p><%= restaurant.city.name %></p>
    <%= cl_image_tag(restaurant.photo, width: 300, height: 200, crop: :fill) %>
  </section>
<% end %>
